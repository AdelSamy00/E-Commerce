/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SalePipe } from './sale.pipe';

describe('Pipe: Salee', () => {
  it('create an instance', () => {
    let pipe = new SalePipe();
    expect(pipe).toBeTruthy();
  });
});
